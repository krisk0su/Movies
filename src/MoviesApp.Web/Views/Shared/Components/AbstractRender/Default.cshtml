@model AbstractRenderViewModel
<div class="row">
    @foreach (var entity in Model.Entities)
    {
        @*<figure class="col-sm-12 col-md-4 col-lg-3 col-xl-2 ">
                <a asp-controller="Movies" asp-action="Details" asp-route-id="@movie.Id">
                    <img src="@movie.Poster"
                         alt="Image"
                         class="img-fluid card-img-top movie-img" />
                    <figcaption class="figure-caption" style="background-color: aliceblue">@movie.Name</figcaption>
                </a>
            </figure>*@
        <div class="col-sm-12 col-md-4 col-lg-3 col-xl-2 d-flex align-items-stretch p-2" style="background-color: #222831">
            <div class="card border-0 shadow-sm">
                <a asp-controller="@Model.Pager.ControllerName" asp-action="Details" asp-route-id="@entity.Id">
                    <img src="@entity.Poster"
                         alt="Image"
                         class="img-fluid card-img-top movie-img" />
                </a>
                <div class="card-body pb-0 pt-2 pl-1 pr-1" style="background-color:#393e46">
                    <h5 class="card-title text-center">
                        <a asp-controller="@Model.Pager.ControllerName" asp-action="Details"
                           asp-route-id="@entity.Id" class="card-link" style="color: #00adb5">@entity.Name</a>
                    </h5>
                </div>
            </div>
        </div>
    }
</div>
@await Component.InvokeAsync("Pager",
    new
    {
        pager = this.Model.Pager
    });

